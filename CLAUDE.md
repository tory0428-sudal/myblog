# myblog

마크다운 파일을 읽어서 정적 블로그 웹사이트로 변환하는 프로젝트.

## 목표

- 마크다운(`.md`)으로 글을 작성하면 블로그 형태의 HTML 페이지로 변환/표시
- 깔끔하고 읽기 좋은 타이포그래피 중심 디자인
- 다크 모드 지원 (시스템 설정 감지 + 수동 토글)
- 모바일 반응형 (좁은 화면에서도 읽기 편할 것)

## 기술 스택 / 제약사항

- **프레임워크 없음**: React, Vue, Next.js 등 사용하지 않음
- HTML, CSS, JavaScript(바닐라)만 사용
- 별도의 번들러/빌드 도구(Webpack, Vite 등) 없이 동작하는 것을 기본으로 함
- 외부 라이브러리를 쓰더라도 최소화하고, 쓰더라도 CDN 스크립트 한두 개 수준으로 제한 (예: 마크다운 파서)

## 프로젝트 구조 (제안)

```
myblog/
├── index.html          # 글 목록 페이지
├── post.html           # 개별 글 렌더링 페이지 (또는 글마다 정적 생성)
├── posts/              # 마크다운 원본 글
│   └── *.md
├── css/
│   └── style.css       # 라이트/다크 테마 포함
├── js/
│   ├── main.js          # 글 목록 로딩/렌더링
│   └── markdown.js      # 마크다운 → HTML 변환 로직
└── assets/              # 이미지 등 정적 리소스
```

세부 구조는 구현하면서 조정 가능. 마크다운을 빌드 타임에 미리 HTML로 변환할지, 클라이언트에서 fetch 후 즉시 렌더링할지는 착수 시 결정.

## 디자인 가이드라인

- 본문 가독성 우선: 적절한 줄간격, 본문 최대 너비 제한(예: 65~75ch), 넉넉한 여백
- 다크 모드는 `prefers-color-scheme` 미디어 쿼리를 기본으로 하되, 사용자가 수동으로 전환할 수 있는 토글 제공 (localStorage에 선택값 저장)
- 모바일: 뷰포트 메타 태그 필수, 폰트 크기·여백을 브레이크포인트별로 조정, 가로 스크롤 발생 금지
- 불필요한 애니메이션/장식 요소 지양, 콘텐츠에 집중하는 미니멀한 톤

## 코딩 컨벤션

- 시맨틱 HTML 태그 사용 (`article`, `header`, `nav`, `time` 등)
- CSS는 커스텀 프로퍼티(변수)로 색상/간격 테마화하여 다크모드 전환을 쉽게 처리
- JS는 모듈 단위로 분리하되 과도한 추상화 지양
